global:
  scrape_interval: 5s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: omo-core
    static_configs:
      - targets: ["omo-host:8080"]
        labels:
          env: "dev"
